# Mobile Devices and Wifi
- Wi-Fi radios use less power than cellular radios, connecting to Wi-Fi networks conserves battery power
These precautions should be taken to protect Wi-Fi communications on mobile devices:
- Never send login or password information using unencrypted text (plaintext).
- Use a VPN connection when possible if you are sending sensitive data.
- Enable security on home networks.
- Use WPA2 or higher encryption for security.

If your device doesn't promptly connect to a wifi network then the network SSID(Service Set Identifier) broadcast may be turned off 
- An **SSID** is the name of a Wi-Fi network
- The wireless AP sends out regular **beacon frames** over the air
- SSID broadcasts typically occur **10 times per second** (every 100 milliseconds), depending on the router configuration.
	- The beacon frame includes key details like:
        - **SSID (Network Name)**: The name of your network.
        - **Supported Network Types**: Protocols like 802.11a/b/g/n/ac/ax.
        - **Security Information**: Whether the network uses encryption (e.g., WPA2, WPA3).
- Nearby devices (phones, laptops, etc.) continuously scan for Wi-Fi networks by listening for beacon frames.
- When a device detects a beacon frame with an SSID, it displays the network in the list of available Wi-Fi networks
- When a user selects the SSID and enters the correct password (if required), the device sends an **association request** to the router or access point
## Simple connectivity with bluetooth
